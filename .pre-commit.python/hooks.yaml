black:
    cmd: black
    flags:
    check: --check
mdformat:
    cmd: mdformat
    flags: --wrap=88
    check: --check
mypy:
    cmd: mypy
    flags: --cache-dir=/tmp/mypy_cache --ignore-missing-imports --install-types --no-color-output --non-interactive
    check:
ruff:
    cmd: ruff
    flags: --config /usr/share/pycheck/ruff.toml --fix
    check: --config /usr/share/pycheck/ruff.toml
